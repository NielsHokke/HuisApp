<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="nl.nielshokke.huisapp.MainActivity$PlaceholderFragment">


    <ImageView
        android:id="@+id/floorView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/floor1" />

    <ImageView
        android:id="@+id/maskView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/mapfloor1"
        android:visibility="invisible" />

    <TextView
        android:id="@+id/HW_TV"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@id/floorView"
        android:layout_alignRight="@id/floorView"
        android:layout_alignTop="@id/floorView"
        android:gravity="center"
        android:text="Floor 1"
        android:textSize="30sp"
        android:textStyle="bold" />


</RelativeLayout>
